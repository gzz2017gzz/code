import Vue from 'vue'
import Vuex from 'vuex'
import  ${upp}  from '../pages/${model}/${low}/${upp}';
Vue.use(Vuex)
export default new Vuex.Store({
  modules: {
    ${lowUpp}: ${upp},
  }
})

