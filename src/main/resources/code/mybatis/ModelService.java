package ${pName};

import com.baomidou.mybatisplus.extension.service.impl.ServiceImpl;
import org.springframework.stereotype.Service;

/**
 * @类说明 【${cName}】业务逻辑实现
 * @author ${auth}
 * @date ${time}
 **/
//@Slf4j
@Service
public class ${upp}Service extends ServiceImpl<I${upp}Dao, ${upp}> {
//    @Autowired
//    private I${upp}Dao dao;//注入[${cName}]数据访问接口
}